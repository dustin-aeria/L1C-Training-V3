"use client";

// Placeholder component - Tina editing is handled via /admin route and Tina Cloud
export default function TinaProvider({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
